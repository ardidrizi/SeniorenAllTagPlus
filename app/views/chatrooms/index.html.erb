  <div class="msg-icon">
      <h1>
        <i class="fa-solid fa-message" style="color: #b9f2df;"></i>
      </h1>
  </div>

<div class="activity--card ">
  <% @chatrooms.each do |chatroom| %>
    <%= link_to chatroom_path(chatroom) do %>
    <div class="card-product">
      <%= cloudinary_or_active_storage_image_tag(
        chatroom.activity.photos.first,
        default: "logo.png",
        class: "acticity-photo",
        alt: "activity photo"
      ) %>
    <div class="card-product-infos">
        <h4><%= chatroom.activity.name %></h4>
        <p><%= chatroom.activity.address %>📌</p>
    </div>
    </div>
    <% end %>
<% end %>
</div>
